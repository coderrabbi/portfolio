import React from 'react';
import Repeat from '../Repeat/Repeat';

const Blog = () => {
    return (
        <div>
            <Repeat title={'Blog'} subHeading={'My Bolg'}></Repeat>
        </div>
    );
};

export default Blog;